export type UpdatedUser = {
    userId: number,
    name: string,
    bio: string | null,
    website: string | null,
    profilePhoto: string,
    backgroundPhoto: string,
    location: string | null,
}